server:
  port: 8081

spring:
  application:
    name: protected-service
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/keycloak-rest-proper-way
